7. Definir una clase simple llamada Data con un miembro de instancia público llamado value de tipo int. Crear e inicializar una variable Data llamada d dentro del método main de una clase TestClass. Crear un array de instancias de Data de longitud 3 e inicializar cada uno de sus elementos con la misma instancia de Data. Usar cualquiera de los elementos del array para actualizar el campo value del objeto Data. Imprimir el valor del campo value del objeto Data usando los tres elementos del array. Por último, imprimir el campo value usando la variable d.

R: Se muestra el mismo valor para todos, debido a que es el mismo objeto referenciado en la variable d y en los elementos del array.